# Used by `npm run build` (and therefore `npm run deploy`). Not a secret: this is the
# public backend URL the deployed site calls. In development this file is ignored and
# VITE_API_URL stays empty, so calls go same-origin and hit the proxy in vite.config.js.
VITE_API_URL=https://twinfin.onrender.com
